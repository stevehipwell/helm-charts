apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "clamav.fullname" . }}
  labels:
    {{- include "clamav.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}.conf: |-
    {{- $value | nindent 4 }}
  {{- end }}
