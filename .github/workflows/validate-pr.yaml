name: Validate PR

on:
  pull_request:
    branches:
      - main
      - release-*
    paths:
      - charts/**

jobs:
  workflow:
    name: Reusable Workflow
    uses: action-stars/helm-workflows/.github/workflows/validate.yaml@bdfbe9eba27acf34b64afc55b1d3db67acd13b07 # v0.4.0
    permissions:
      contents: read
      id-token: write
      pull-requests: write
    with:
      chart_dir: charts
      target_branch: main
      helm_docs: true
      changelog: true
      ah_lint: true
      kubeconform: true
      kubeconform_k8s_versions: ${{ vars.K8S_VERSIONS }}
      helm_extra_args: "--timeout 600s"
