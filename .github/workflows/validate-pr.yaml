name: Validate PR

on:
  pull_request:
    branches:
      - main
      - release-*
    paths:
      - charts/**

jobs:
  workflow:
    name: Reusable Workflow
    uses: action-stars/helm-workflows/.github/workflows/validate.yaml@2835f07808aed3d1115b062a2a11299b01bad6a2 # v0.5.1
    permissions:
      contents: read
      id-token: write
      pull-requests: write
    with:
      chart_dir: charts
      target_branch: main
      helm_docs: true
      changelog: true
      ah_lint: true
      kubeconform: true
      kubeconform_k8s_versions: ${{ vars.K8S_VERSIONS }}
      helm_extra_args: "--timeout 600s"
