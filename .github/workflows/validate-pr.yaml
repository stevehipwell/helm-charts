name: Validate PR

on:
  pull_request:
    branches:
      - main

concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

permissions: read-all

jobs:
  setup:
    name: Setup
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    outputs:
      changed: ${{ steps.changed.outputs.any_changed }}
      k8s_versions: ${{ steps.setup.outputs.k8s_versions }}
    steps:
      - name: Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 2

      - name: Chart changes
        id: changed
        uses: tj-actions/changed-files@24d32ffd492484c1d75e0c0b894501ddb9d30d62 # v47.0.0
        with:
          files: charts/**

      - name: Setup
        id: setup
        run: |
          set -euo pipefail

          k8s_versions="$(curl -s https://endoflife.date/api/kubernetes.json | jq -r '[sort_by(.releaseDate) | reverse | .[0:4] | "\(.[].cycle).0"] | join(",")')"
          echo "k8s_versions=${k8s_versions}" >> "${GITHUB_OUTPUT}"

  validate:
    name: Validate
    uses: action-stars/helm-workflows/.github/workflows/validate.yaml@1f7bf2a447a90824395c0ed2428419e660a420ab # v0.8.10
    needs: setup
    if: needs.setup.outputs.changed == 'true'
    permissions:
      contents: read
      id-token: write
      pull-requests: write
    with:
      chart_dir: charts
      target_branch: main
      helm_docs: true
      helm_docs_args: --template-files=./_templates.gotmpl --template-files=./_chart-readme.md.gotmpl
      changelog: true
      ah_lint: true
      kubeconform: true
      kubeconform_k8s_versions: ${{ needs.setup.outputs.k8s_versions }}
      helm_extra_args: "--timeout 600s"

  check:
    name: Check
    needs: validate
    if: always()
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Check
        run: |
          set -euo pipefail

          if [[ "${{ needs.validate.result }}" == "failure" ]] || [[ "${{ needs.validate.result }}" == "cancelled" ]]; then
            echo "::error::Workflow failed!"
            exit 1
          fi
